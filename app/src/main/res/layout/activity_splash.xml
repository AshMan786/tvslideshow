<?xml version="1.0" encoding="utf-8"?>
<!-- Cinematic Luxury Splash Screen -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/splash_background">

    <!-- Centered content with elegant vertical layout -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:orientation="vertical"
        android:gravity="center">

        <!-- Golden Aperture Icon -->
        <View
            android:id="@+id/splash_icon"
            android:layout_width="140dp"
            android:layout_height="140dp"
            android:background="@drawable/splash_icon"
            android:layout_marginBottom="40dp"
            android:alpha="0" />

        <!-- App Title - Elegant Typography -->
        <TextView
            android:id="@+id/splash_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/app_name"
            android:textSize="52sp"
            android:textColor="@color/text_primary"
            android:fontFamily="serif"
            android:letterSpacing="0.15"
            android:alpha="0" />

        <!-- Subtle golden divider -->
        <View
            android:id="@+id/splash_divider"
            android:layout_width="80dp"
            android:layout_height="2dp"
            android:background="@drawable/divider_gold"
            android:layout_marginTop="16dp"
            android:layout_marginBottom="16dp"
            android:alpha="0" />

        <!-- Tagline with warm cream color -->
        <TextView
            android:id="@+id/splash_subtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/splash_subtitle"
            android:textSize="16sp"
            android:textColor="@color/text_secondary"
            android:fontFamily="sans-serif-light"
            android:letterSpacing="0.08"
            android:alpha="0" />

    </LinearLayout>

    <!-- Bottom branding accent -->
    <View
        android:layout_width="match_parent"
        android:layout_height="2dp"
        android:layout_gravity="bottom"
        android:background="@drawable/bottom_accent_line"
        android:alpha="0.5" />

</FrameLayout>